spring:
  datasource:
    url: jdbc:h2:mem:aurora-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
aurora:
  demo-data: true
  security:
    jwt-secret: AuroraPizzaOS-test-secret-with-more-than-thirty-two-characters
    token-hours: 1
